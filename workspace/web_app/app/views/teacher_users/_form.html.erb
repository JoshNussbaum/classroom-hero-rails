<%= form_for(@teacher_user) do |f| %>
  <% if @teacher_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher_user.errors.count, "error") %> prohibited this teacher_user from being saved:</h2>

      <ul>
      <% @teacher_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coins %><br>
    <%= f.number_field :coins %>
  </div>
  <div class="field">
    <%= f.label :leagueId %><br>
    <%= f.number_field :leagueId %>
  </div>
  <div class="field">
    <%= f.label :hasStamps %><br>
    <%= f.check_box :hasStamps %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
